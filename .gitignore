# Examples 1 and 2 show that only source/config files were added (README.md), which should result in an empty .gitignore.
# Since only a README.md file was added and no build artifacts, dependencies, or temp files are present, no ignores are needed.
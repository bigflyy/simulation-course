<Window x:Class="SimulationLabs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Лабораторная работа 9: СМО M/M/1/0 (с потерями)"
        Width="950" Height="650"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ParamLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ParamBox" TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Width" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Система массового обслуживания M/M/1/0 (с потерями)"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Панель параметров -->
        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,10" Background="#F9F9F9">
            <StackPanel>
                <TextBlock Text="Параметры модели" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource ParamLabel}" Text="λ (прибытие):"/>
                    <TextBox x:Name="txtLambda" Style="{StaticResource ParamBox}" Text="4"/>
                    <TextBlock Style="{StaticResource ParamLabel}" Text="μ (обслуживание):"/>
                    <TextBox x:Name="txtMu" Style="{StaticResource ParamBox}" Text="5"/>
                    <TextBlock Style="{StaticResource ParamLabel}" Text="N (экспериментов):"/>
                    <TextBox x:Name="txtN" Style="{StaticResource ParamBox}" Text="1000"/>
                    <Button x:Name="btnRun" Content="▶ Запустить" Margin="20,0,0,0"
                            Padding="20,8" FontSize="13" FontWeight="Bold"
                            Background="#90EE90" BorderBrush="#4CAF50" BorderThickness="1"
                            Click="BtnRun_Click"/>
                </WrapPanel>
                <TextBlock x:Name="lblRho" Text="ρ = 0.800" FontWeight="SemiBold" Margin="0,8,0,0" Foreground="#333"/>
            </StackPanel>
        </Border>

        <!-- Основной контент -->
        <lvc:CartesianChart x:Name="chartDist" Grid.Row="2" DisableAnimations="True" LegendLocation="Right">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Состояние системы"/>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>

        <!-- Статус -->
        <TextBlock x:Name="lblStatus" Grid.Row="3" Text="Готово к запуску" FontWeight="SemiBold" Margin="0,5,0,0"/>
    </Grid>
</Window>
